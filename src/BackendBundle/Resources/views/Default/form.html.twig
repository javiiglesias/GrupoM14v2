{% extends "BackendBundle:Default:base.html.twig" %}
{% block title %}Form{% endblock %}
{% block content %}
        <div class="col-lg-12">
            <div class="content-backend">
                <div class="col-lg-11">
                    <h2 class="text-center">{{ titol }}</h2>
                    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-7 col-lg-push-4">
                        {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}
                        {{ form_widget(form)}}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
{% endblock %}